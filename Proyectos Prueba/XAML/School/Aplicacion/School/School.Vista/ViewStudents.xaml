<Window x:Class="School.Vista.ViewStudents"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="View Students" Height="327.311" Width="400" Loaded="Window_Loaded_1">
    <Grid>
        <DataGrid HorizontalAlignment="Left" Height="204" Margin="10,40,0,0" VerticalAlignment="Top" Width="372" Name="StudentGrid" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="STUDENTID" Binding="{Binding Path=STUDENTID}"/>
                <DataGridTextColumn Header="FIRSTNAME" Binding="{Binding Path=FIRSTNAME}"/>
                <DataGridTextColumn Header="LASTNAME" Binding="{Binding Path=LASTNAME}"/>
                <DataGridTextColumn Header="GENDER" Binding="{Binding Path=GENDER}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="View Current Classes" HorizontalAlignment="Left" Margin="10,265,0,0" VerticalAlignment="Top" Width="146"/>
        <Button Content="View Finish Classes" HorizontalAlignment="Left" Margin="236,265,0,0" VerticalAlignment="Top" Width="146"/>
    </Grid>
</Window>
