@using System.Globalization
@model Universidad.Entidades.US_USUARIOS
@{
    ViewBag.Title = "Crear cuenta de usuario";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Head
{
    <script src="/Scripts/bootstrap.js" type="text/javascript"></script>
    <link href="/Content/bootstrap.css" type="text/css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap-select.js" type="text/javascript"></script>
    <link href="~/Content/bootstrap-select.css" type="text/css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap-multiselect.js" type="text/javascript"></script>
    <link href="~/Content/bootstrap-multiselect.css" type="text/css" rel="stylesheet" />
}

@section Scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            $('.selectpicker').selectpicker({
                style: 'btn-default btn-sm'
            });

            $('#txtContrasena').change(function () {
                var c = $('#txtContrasena').val();
                var cc = $('#txtConfirmaContrasena').val();

                if (c == cc && (c != "" && cc != "")) {
                    $("#divContrasena").removeClass('btn-success').addClass('btn-danger');
                    $("#divContrasena").addClass('has-success').removeClass('has-error');
                } else {
                    $("#divContrasena").removeClass('btn-danger').addClass('btn-success');
                    $("#divContrasena").addClass('has-error').removeClass('has-success');
                    $("#btnEditar").attr("disabled", "disabled");
                }
            });

            $('#txtConfirmaContrasena').change(function () {
                var c = $("#txtContrasena").val();
                var cc = $("#txtConfirmaContrasena").val();

                if (c == cc && (c != "" && cc != "")) {
                    $("#divContrasena").removeClass('btn-success').addClass('btn-danger');
                    $("#divContrasena").addClass('has-success').removeClass('has-error');
                    $("#btnEditar").removeAttr("disabled", "disabled");
                } else {
                    $("#divContrasena").removeClass('btn-danger').addClass('btn-success');
                    $("#divContrasena").addClass('has-error').removeClass('has-success');
                    $("#btnEditar").attr("disabled", "disabled");
                }
            });
        });



        function EditaUsuario() {
            var usuario = $("#txtUsuario").val();
            var contrasena = $("#txtContrasena").val();
            var tipoUsuario = $("#txtTipoUsuario").val();
            var nivelUsuario = $("#txtNivelUsuario").val();
            var estatusUsuario = $("#txtEstatusUsuario").val();

            var idUsuario = '@Model.ID_USUARIO.ToString(CultureInfo.InvariantCulture)';
            $.ajax({
                type: "POST",
                data: {
                    'usuario': usuario,
                    'contrasena': contrasena,
                    'tipoUsuario': tipoUsuario,
                    'nivelUsuario': nivelUsuario,
                    'estatusUsuario': estatusUsuario,
                    'idUsuario': idUsuario
                },
                url: '@Url.Action("EditaCuentaUsuario", "Usuarios")',
                success: function (response) {
                    if (response == "0") {
                        alert("Error al crear cuenta de usuario");
                    } else {
                        alert("Se creo cuenta de usuario " + response);
                    }
                },
                error:
                    function (xhr, status, error) {
                        alert(status + " : " + error);
                    }
            });
        }
    </script>
}
<div class="jumbotron">
    <br />
    <h2>@ViewBag.Title</h2>
    <br />

    <div class="panel panel-default form-margin">
        <div class="panel-heading form-margin">
            <div class="panel-title">Llena los datos del usuario</div>
        </div>
        <fieldset></fieldset>
        <div class="form-group row form-group input-group-sm" style="margin: 5px; padding: 10px;">
            <div class="row">
                <div class="row" style="margin: 0 0 0 0; padding: 0 0 0 0;">
                    <div class="form-group col-md-4">
                        <div class="input-group">
                            @Html.Label("Estatus de usuario", null, new { @class = "input-group-addon" })
                            @Html.DropDownListFor(r => r.ID_ESTATUS_USUARIOS, (IEnumerable<SelectListItem>)ViewBag.ListaEstatusUsuario, new { id = "txtEstatusUsuario", @class = "selectpicker", data_live_search = true })
                        </div>
                    </div>
                    <div class="form-group col-md-4">
                        <div class="input-group">
                            @Html.Label("Tipo de usuario", null, new { @class = "input-group-addon" })
                            @Html.DropDownListFor(r => r.ID_TIPO_USUARIO, (IEnumerable<SelectListItem>)ViewBag.ListaTipoUsuario, new { id = "txtTipoUsuario", @class = "selectpicker", data_live_search = true })
                        </div>
                    </div>
                    <div class="form-group col-md-4">
                        <div class="input-group">
                            @Html.Label("Nivel de usuario", null, new { @class = "input-group-addon" })
                            @Html.DropDownListFor(r => r.ID_NIVEL_USUARIO, (IEnumerable<SelectListItem>)ViewBag.ListaNivelUsuario, new { id = "txtNivelUsuario", @class = "selectpicker", data_live_search = true })
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="row" style="margin: 0 0 0 0; padding: 0 0 0 0;">
                    <div class="form-group col-md-4">
                        <div class="input-group" id="divUsuario">
                            @Html.Label("Usuario", new { @class = "input-group-addon" })
                            @Html.TextBoxFor(r => r.USUARIO, "", new { @class = "form-control", @id = "txtUsuario", placeholder = "Nuevo Usuario", autocomplete = "off", disabled = "disabled" })
                        </div>
                    </div>
                    <div class="form-group col-md-8">
                        <div class="input-group" id="divContrasena">
                            @Html.Label("Nueva Contraseña", new { @class = "input-group-addon" })
                            @Html.TextBoxFor(r => r.CONTRASENA, "", new { @id = "txtContrasena", @class = "form-control", @type = "password", placeholder = "Nueva contraseña", autocomplete = "off" })
                            @Html.Label("Confirma contraseña", new { @class = "input-group-addon" })
                            @Html.TextBoxFor(r => r.CONTRASENA, "", new { @id = "txtConfirmaContrasena", @class = "form-control", @type = "password", placeholder = "Confirma Contraseña", autocomplete = "off" })
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div style="margin-top: 10px; float: right;" class="form-group">
            <label>&nbsp;</label>
            <input type="button" id="btnEditar" value="Editar cuenta" class="btn btn-info btn-sm" style="margin: 10px; padding: 10px;" onclick="EditaUsuario();">
        </div>
    </div>
</div>